# Copy this to .env and fill in values (never commit .env)

# Optional: Prefect Cloud API key (leave blank to use local Prefect server)
PREFECT_API_KEY=
PREFECT_API_URL=

# Optional: override default data paths
# BRFSS_DATA_DIR=/path/to/data
# DUCKDB_PATH=/path/to/brfss.duckdb
